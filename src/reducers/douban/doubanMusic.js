const initState = {
    results: {},
  };
    
  const doubanMusicReducer = (state = initState, action) => {
    switch (action.type) {
    
      default:
        return state;
    }
  };
  
  export default doubanMusicReducer;