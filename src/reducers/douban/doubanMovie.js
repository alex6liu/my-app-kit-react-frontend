const initState = {
    results: {},
  };
    
  const doubanMovieReducer = (state = initState, action) => {
    switch (action.type) {
    
      default:
        return state;
    }
  };
  
  export default doubanMovieReducer;